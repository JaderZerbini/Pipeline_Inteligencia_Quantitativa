# requirements-actions.txt — deps for GitHub Actions (Linux / Python 3.12)
# Used by both crypto_pipeline.yml and b3_pipeline.yml.
# Do NOT add streamlit or other dashboard-only packages here.
#
# --- shared (crypto + B3) ---
requests>=2.28.0
python-dotenv>=1.0.0
python-telegram-bot>=20.0
google-genai>=1.0.0
openai>=1.0.0
psycopg2-binary>=2.9.9

# --- B3 pipeline runtime (main.py) ---
# Pinned to the versions in requirements.txt so Actions matches local.
pandas==3.0.2
numpy==2.2.6
pandas-ta>=0.3.14b
yfinance==1.3.0
feedparser==6.0.12
pytz==2026.2
